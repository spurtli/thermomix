import { Client } from "../../src/types";

export const client: Client = {
  id: "react-oauth-provider-client",
  authorizeURL: "https://api.example.org/authorize",
  refreshURL: "https://api.example.org/auth/token"
};
